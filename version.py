"""Canonical app version. Overwritten (not committed back) by
.github/workflows/ci.yml's compute-version job during a release build —
this file's committed value is only ever seen running from source.
"""

APP_VERSION = "0.0.0-dev"
